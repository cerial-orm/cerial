/**
 * Tuple derived type generator - generates Select types for tuples
 *
 * Tuples have simpler derived types than objects:
 * - Select: boolean only (no per-position select)
 * - No OrderBy (tuples cannot be ordered by)
 */

/** Placeholder — tuples use boolean-only select, no dedicated Select type needed */
