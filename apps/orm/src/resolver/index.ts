/**
 * Resolver module — extends inheritance resolution utilities
 */

export { applyElementFilter, applyFieldFilter, applyValueFilter, applyVariantFilter } from './filter';
export { resolveInheritance } from './inheritance-resolver';
