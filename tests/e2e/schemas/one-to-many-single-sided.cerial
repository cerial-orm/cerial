# Only Comment defines relation, Article has no reverse
model Article {
  id String @id
  title String
}

model Comment {
  id String @id
  text String
  articleId Record?
  article Relation? @field(articleId) @model(Article)
}
