# 1-1 Optional with @onDelete Restrict
model UserRestrict {
  id String @id
  name String
  profile Relation? @model(ProfileRestrict)
}

model ProfileRestrict {
  id String @id
  bio String?
  userId Record?
  user Relation? @field(userId) @model(UserRestrict) @onDelete(Restrict)
}
