# 1-n Optional Bidirectional
model Publisher {
  id Record @id
  name String
  books Relation[] @model(Book)
}

model Book {
  id Record @id
  title String
  isbn String @unique
  publisherId Record? @nullable
  publisher Relation? @field(publisherId) @model(Publisher)
}
