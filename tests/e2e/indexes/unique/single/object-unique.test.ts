/**
 * E2E Tests: @unique on object-typed fields
 *
 * TODO: Requires a schema with @unique on an object field (e.g., address Address @unique).
 * This is supported by the generator but not yet tested with a dedicated schema.
 */
